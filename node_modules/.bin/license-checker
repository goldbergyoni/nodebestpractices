#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../license-checker/bin/license-checker" "$@"
  ret=$?
else 
  node  "$basedir/../license-checker/bin/license-checker" "$@"
  ret=$?
fi
exit $ret
